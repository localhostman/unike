import { ProductSupportDeliveryPipe } from './product-support-delivery.pipe';

describe('ProductSupportDeliveryPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductSupportDeliveryPipe();
    expect(pipe).toBeTruthy();
  });
});
